[NIS(CN).md](https://github.com/user-attachments/files/31693973/NIS.CN.md)
# 嵌套积分结构完整描述

**摘要**：本文建立一类以变限积分迭代为基本运算的动态系统模型（Nested Integral Structure, NIS）。核心规则为：外层积分的上下限由前一轮积分的数值结果动态决定，运算严格由内向外逐层推进。原始形态为有限层数的多重嵌套积分，向"递推收敛"与"累加求和"两个方向拓展，并在统一的状态更新形式下进行描述，静态级数可作为其退化情形。本文运用压缩映射原理与摄动可和性条件，给出迭代序列收敛的充分条件，建立不动点的存在唯一性定理，并分析标准压缩与临界情形的渐近行为。该框架可用于建模前序输出决定后续运算边界的动态演化过程。本文不主张建立新的压缩映射理论或替代已有非自治动力系统理论。

**关键词**：嵌套积分结构；压缩映射；摄动可和性；不动点；收敛性；变限积分迭代

**Abstract**: This paper introduces a dynamical-system framework based on the iteration of variable-limit integrals. The defining mechanism is that the integration bounds at one layer are determined by the numerical output of the preceding inner layer, enforcing an inside-out computational structure. Starting from finite-depth nested integrals, the framework is developed along two analytical directions: recursive iteration and cumulative summation. These components are then organized into a unified state-update formulation for dynamical processes in which preceding states determine the operational boundaries of subsequent integral transformations. Sufficient conditions for convergence, fixed-point existence and uniqueness, and asymptotic behavior are established using contraction mapping and perturbation summability conditions.

**声明**：本文的数学框架与核心结论由作者提出并完成；语言润色、排版整理及多轮交叉审校借助了 AI 工具。所有关键数学命题均经过多轮交叉审查与作者复核。

**利益冲突声明**：作者声明，就本文的发表不存在利益冲突。

---

## 一、核心思想与原始原型

### 1.1 核心思想

外层积分的上下限由内层积分的数值计算结果直接决定，运算严格由内向外逐层推进（因外层积分域依赖内层结果，积分计算顺序不可交换）。以七重嵌套为基础，向"递推收敛"与"累加求和"两个方向拓展，建立统一的状态更新描述框架，用于描述一类前一步状态决定后续积分边界的动态演化过程，在满足相应收敛条件时，可进一步讨论与经典无限嵌套表达式类似的极限结构。

**本文定位**：本文主要关注动态边界嵌套积分结构的形式化定义、基本性质及统一收敛框架。本文不主张建立新的压缩映射理论或替代已有非自治动力系统理论，而是将变限积分迭代这一具体结构置于相关理论框架下进行系统化描述，并分析其特有的积分边界依赖机制。后续工作将在此基础上研究更具体的动力系统性质、推广结构及应用模型。

### 1.2 原始原型：有限 $N$ 重嵌套积分

$$
\begin{cases}
I_N = \displaystyle\int_{L_N}^{U_N} f_N(t_N) \, dt_N \\
I_k = \displaystyle\int_{L_k(I_{k+1})}^{U_k(I_{k+1})} f_k(t_k) \, dt_k, \quad k = N-1, \dots, 1
\end{cases}
$$

最终结果 $I^{(N)} = I_1$。当 $N = 7$ 时，即为原始七重嵌套积分。

**下标约定与对应关系**：下标 $k$ 越大，对应积分层级越靠内；$I_N$ 为最内层积分（最先计算），$I_1$ 为最外层积分（最后计算），计算顺序严格为 $I_N \to I_{N-1} \to \cdots \to I_1$。对于由同一积分映射反复作用构成的自治递推表示（$L_n \equiv L, U_n \equiv U, f_n \equiv f$），递推序列以 $I_0$ 作为零次迭代的初始状态；经过 $n$ 次映射后得到 $I_n$。若约定 $I^{(0)} := I_0$ 表示零层初始状态，则自治情形下有 $I_n = I^{(n)}$。若将最内层积分结果定义为第一层 $I^{(1)}$，则相应记为 $I_n = I^{(n+1)}$。为避免差一歧义，本文后续统一采用前一种约定，即

$$
I^{(0)} := I_0, \qquad I^{(n+1)} = F\bigl(I^{(n)}\bigr), \quad n \ge 0,
$$

从而 $I_n = I^{(n)}$。

**与经典嵌套积分的本质区别**：传统多重积分的积分区域是预先给定的固定常数，各层积分可一次性独立完成，在满足 Fubini--Tonelli 等相应条件时，相关积分次序才可交换。本文结构的核心差异在于积分边界由内层积分的数值结果动态生成——外层积分的上下限 $L_k, U_k$ 直接依赖于内层输出 $I_{k+1}$，形成"结果 $\to$ 边界 $\to$ 下一层结果"的闭环。因此在该依赖结构下，计算通常必须严格由内向外推进；不能像固定积分区域那样直接依据 Fubini--Tonelli 条件交换相关层次。该动态边界机制构成本文结构与经典固定区域多重积分之间的主要区别。

### 1.3 拓展：无限嵌套积分

**定义 1.3（无限嵌套积分）**：若对任意正整数 $N$，1.2 节的有限嵌套结构均可良定义，从而得到截断值序列 $\{I^{(N)}\}_{N=1}^\infty$，则当该序列收敛时，定义其极限为无限嵌套积分值：

$$
I^* := \lim_{N \to \infty} I^{(N)}
$$

若序列发散（即极限不存在），则该无限嵌套不收敛，本文不讨论此情形。收敛的充分条件见 2.2 节（自治情形）与 4.1 节（非自治混合情形）；充要条件的完整刻画仍有待进一步研究。

注：非自治情形下，$N$ 重截断对应按"由内向外"计算顺序复合 $F_1 \circ F_2 \circ \cdots \circ F_{N-1}$（最内层结果先经 $F_{N-1}$ 作用，逐层向外，最终由 $F_1$ 输出）；增加一层时，在现有复合结构的最内侧增加新的层级映射 $F_N$。自治情形增加层数等价于迭代多走一步。有限嵌套的"由内向外层级推进"与无限递推的"由初始状态向前迭代"是两个不同的指标方向，移植时需注意区分。

### 1.4 不动点方程的符号推导

若自治有限截断序列 $\{I^{(N)}\}$ 收敛，且映射 $F$ 连续，则取极限得到形式不动点方程。本小节仅为形式推导；不动点的存在唯一性由后续压缩映射定理给出。

仅在自治情形下，由 $I^{(N+1)} = F(I^{(N)})$（此处 $F$ 不随层数变化），两边取 $N \to \infty$ 极限，结合 $F$ 连续性可得核心不动点方程：

$$
\boxed{I^* = F(I^*)}
$$

非自治情形无统一不动点方程（因 $F_n$ 随层数变化）。若满足第 4.1 节的相邻摄动可和性与统一压缩等条件，则 $F_n$ 一致收敛到极限映射 $F$，此时迭代极限满足 $I^* = F(I^*)$；但这属于额外收敛条件下得到的极限映射不动点关系，并非一般非自治情形本身自带的性质。

其思想与经典无限嵌套表达式的"不动点自洽"思路具有类比性，但本文的收敛性必须由相应的积分映射条件独立证明。

### 1.5 通用基础假设

为避免后续各章重复声明，将全文默认前提集中如下：

- **(H1)** 所有被积函数 $f_n, f_{n,k}$ 在相应积分区间上连续；若积分区间为无界区间，则相应广义积分按通常定义存在。
- **(H2)** 所有边界函数 $L_n, U_n, L_{n,k}, U_{n,k}$ 为 $C^1$ 类（一阶连续可导）函数；
- **(H3)** 在正文的默认"无定向积分"约定下，要求 $L_n(x) \le U_n(x)$、$L_{n,k}(x) \le U_{n,k}(x)$；若采用有向积分约定，则允许上下限倒置，并按 $\int_a^b f = -\int_b^a f$ 定义。工程或物理模型若要求"下界不高于上界"，应将其作为额外的语义约束，而非积分本身的数学合法性条件；
- **(H4)** 迭代定义域为前向不变闭集 $X$（一维情形通常取为闭区间 $[a,b]$，构造方法见 2.1 节），保证映射 $F_n: X \to X$ 不溢出；
- **(H5)** 默认工作空间为一维实数域 $\mathbb{R}$；高维拓展需在明确的范数空间中讨论，并以映射的 Lipschitz 常数或 Jacobian 算子范数建立压缩性，详见附录；
- **(H6)** 误差度量采用绝对值 $\mathrm{dist}(x,y) := |x - y|$（一维情形下欧式距离退化为绝对值，避免 $\rho$ 在度量空间与实数模之间的符号歧义）。

### 符号表

| 符号 | 含义 |
|:---|:---|
| $I^{(N)}$ | $N$ 重有限嵌套积分截断值 |
| $I_n$ | 递推序列第 $n$ 项（自治时 $I_n = I^{(n)}$） |
| $I^*$ | 无限嵌套积分稳态/不动点 |
| $F, F_n$ | 自治/非自治积分映射 |
| $q$ | 全局/最终一致 Lipschitz 压缩常数，$0 \le q < 1$ |
| $\lambda$ | 指数衰减系数，$0 < \lambda < 1$（定理 4.1 注） |
| $\mathrm{dist}(x,y)$ | 一维距离，定义为 $\mathrm{dist}(x,y) := \lvert x - y\rvert$ |
| $q_\infty$ | 渐近 Lipschitz 上确界，$\limsup_{n\to\infty} \sup_{x\in X} \lvert F_n'(x)\rvert$ |
| $\varepsilon_n$ | 相邻摄动上界，$\varepsilon_n := \sup_{x\in X} \lvert F_n(x) - F_{n-1}(x)\rvert$ |
| $\delta_k$ | 第 $k$ 步数值计算单步误差界（5.4 节） |
| $a_k$ | 第 $k$ 步映射逼近误差，$a_k := \sup_{x\in X} \lvert F_k(x) - F(x)\rvert$ |
| $e_n$ | 第 $n$ 步迭代点误差，$e_n := \lvert I_n - I^*\rvert$ |


---

## 二、方向一：递推收敛型（自治迭代）

### 2.1 基本定义

设 $f_n$ 连续，$L_n, U_n$ 为 $C^1$ 类函数。给定初始值 $I_0 \in X$，迭代公式：

$$
I_{n+1} = \int_{L_n(I_n)}^{U_n(I_n)} f_n(t) \, dt, \quad n \ge 0
$$

**前向不变闭集 $X$ 的定义与构造**：$X$ 满足 $F_n(X) \subseteq X$（迭代结果不溢出）。具体构造时通常取 $X = [a,b]$（一维闭区间），要求对任意 $x \in [a, b]$：
1. 积分区间合法：$L_n(x) \le U_n(x)$（由 H3 保证，等号对应退化为零测区间的情形）；
2. 映射不溢出：$F_n(x) \in [a, b]$，即 $F_n: [a, b] \to [a, b]$。

自治情形可通过验证 $\min_{x\in[a,b]} F(x) \ge a$ 与 $\max_{x\in[a,b]} F(x) \le b$ 来确认自映射性；非自治情形则需对所有相关 $n$ 验证 $F_n([a,b]) \subseteq [a,b]$。若只需构造局部不变邻域，可结合具体问题采用半局部分析方法，但这不属于本文定理的必要条件。

**对应关系说明**：当自治结构的最内层固定积分结果被选作初始状态时，重复迭代与不断增加相同外层积分确实等价。对一般非自治结构，有限嵌套对应的是按"由内向外"的层级顺序复合 $F_1 \circ F_2 \circ \cdots \circ F_{N-1}$（按从内向外计算顺序，最内层 $I_N$ 为基准积分值，向外逐层作用 $F_{N-1}, \dots, F_1$，最终由最外层映射 $F_1$ 输出结果）；因此本文不再把"任意非自治递推的 $N$ 步截断"无条件称为原始有限 $N$ 重嵌套，而是将其作为明确规定的非自治递推表示。反之，任给一族仅依赖自身单一输入、不涉及跨层全局依赖的映射 $\{F_k\}_{k=1}^N$，均可按本节格式组织为一个合法的 $N$ 重嵌套积分结构；但这只是 NIS 定义方式本身的适用范围，并不意味着对给定初值 $I_0$ 正向运行得到的非自治递推序列 $\{I_n\}_{n=0}^N$ 自动等于某组固定内层基准值反向展开得到的截断值——两者关于"哪一端是给定的初始数据、哪一端是计算结果"方向相反，一般不能不加说明地等同。

### 2.2 全局压缩收敛定理（完整证明）

**定理（Banach 压缩映射原理 [1]）**：设 $(X, \rho)$ 是完备距离空间，$T: X \to X$ 是压缩映射，即存在常数 $\alpha \in [0, 1)$，使得对任意 $x, y \in X$，有

$$
\rho(Tx, Ty) \le \alpha \rho(x, y)
$$

则 $T$ 在 $X$ 中存在唯一的不动点 $z \in X$，满足 $Tz = z$。

**证明**：

**第一步，构造 Cauchy 序列**。任取初始点 $x_0 \in X$，定义 $x_n = T^n x_0$，$n \ge 0$。首先

$$
\rho(x_1, x_0) = \rho(Tx_0, x_0),
$$

并由压缩条件对任意 $m \ge 1$ 有

$$
\rho(x_{m+1}, x_m) = \rho(Tx_m, Tx_{m-1}) \le \alpha \rho(x_m, x_{m-1}) \le \cdots \le \alpha^m \rho(x_1, x_0).
$$

对任意 $n > m$，由三角不等式：

$$
\rho(x_m, x_n) \le \sum_{k=m}^{n-1} \rho(x_k, x_{k+1}) \le \sum_{k=m}^{n-1} \alpha^k \rho(x_1, x_0) \le \frac{\alpha^m}{1-\alpha} \rho(x_1, x_0)
$$

因 $\alpha < 1$，$m \to \infty$ 时右侧趋于 $0$，故 $\{x_n\}$ 是 Cauchy 序列。

**第二步，利用完备性证明存在性**。因 $X$ 完备，Cauchy 列 $\{x_n\}$ 收敛到某个 $z \in X$，即 $x_n \to z$。由压缩映射的连续性（压缩映射必然连续）：

$$
Tz = T\Big(\lim_{n\to\infty} x_n\Big) = \lim_{n\to\infty} Tx_n = \lim_{n\to\infty} x_{n+1} = z
$$

所以 $z$ 是 $T$ 的不动点。

**第三步，证明唯一性**。设 $y \in X$ 也是 $T$ 的不动点，即 $Ty = y$，则

$$
\rho(y, z) = \rho(Ty, Tz) \le \alpha \rho(y, z)
$$

因 $\alpha < 1$，上式迫使 $\rho(y, z) = 0$，即 $y = z$。唯一性得证。

**推论（误差上界）**：设 $z$ 是 $T$ 的唯一不动点，则对任意 $x_0 \in X$，有

$$
\rho(z, x_m) \le \frac{\alpha^m}{1-\alpha} \rho(Tx_0, x_0)
$$

#### 在嵌套积分结构中的应用

**条件层级说明**：本节针对自治 $C^1$ 可微情形使用经典微积分手段判定压缩性；第 4.1 节主定理将条件弱化至仅需 Lipschitz 连续，兼容非光滑与非自治系统。

**Step A. 映射的可导性推导**。由假设 (H2) 边界函数为 $C^1$ 类、(H3) 积分区间合法，结合变限积分求导的 Leibniz 法则，单步映射 $F(x) = \int_{L(x)}^{U(x)} f(t) \, dt$ 的导数为

$$
F'(x) = f(U(x)) \cdot U'(x) - f(L(x)) \cdot L'(x)
$$

因 $f$ 连续、$L, U$ 为 $C^1$ 函数，故 $F(x)$ 在 $X$ 上一阶连续可导。

**Step B. 压缩性建立**。若存在 $q < 1$ 使得 $|F'(x)| \le q$，由拉格朗日中值定理（注：此处利用一维情形的微分中值定理；高维情形需将 $|F'(x)|$ 替换为 Jacobian 矩阵的算子范数 $\|J_F(x)\|_{\mathrm{op}}$，详见附录）：

$$
|F(x) - F(y)| \le q |x - y|
$$

即 $F$ 是压缩映射。

**Step C. 收敛性结论**。闭区间 $[a,b] \subset \mathbb{R}$ 作为距离空间是完备的，满足 Banach 压缩映射原理的前提。依据该原理，迭代 $I_{n+1} = F(I_n)$ 必然收敛到唯一不动点 $I^*$，且误差上界：

$$
|I_n - I^*| \le \frac{q^n}{1-q} |I_1 - I_0|
$$

注：本定理为 1.4 节不动点方程 $I^* = F(I^*)$ 的存在性和唯一性提供严格的数学保证。

### 2.3 收敛性拓展

1. **局部与半局部收敛**：Ostrowski 型结果 [2] 表明，在适当的局部可微条件下，不动点处 $|F'(I^*)| < 1$ 可推出邻域内线性收敛；Kantorovich 型定理 [3] 则通过导数界、Lipschitz 条件及初始点等半局部信息建立收敛判据。
2. **收敛阶分类**：
   - 线性收敛（$0 < \lvert F'(I^*)\rvert < 1$）
   - 超线性收敛（$F'(I^*) = 0$）
   - 二阶局部收敛（在所述局部正则性假设下 $F'(I^*) = 0$ 且 $F''(I^*) \ne 0$）

   Aitken 的 $\Delta^2$ 方法可以加速适合的线性收敛不动点迭代，而 Steffensen 迭代在适当正则性条件下可以达到局部二次收敛 [6]。
3. **迭代路径特征**：在不动点的适当邻域内，若 $0 < F'(I^*) < 1$，则局部保序并线性收敛；若 $-1 < F'(I^*) < 0$，则局部交替振荡并线性收敛；$|F'(I^*)| = 1$ 为临界情形，局部线性化本身不足以判定收敛性。

### 2.4 失效边界与反例

本节讨论限定于一维实数域 $\mathbb{R}$，$|\cdot|$ 表示实数绝对值，发散指 $|I_n| \to +\infty$。

1. **局部压缩、全局不压缩案例**：取 $f(t) = 2t$，$L(x) = 0$，$U(x) = x$，则 $F(x) = x^2$，$F'(x) = 2x$。在 $x = 0$ 邻域满足 $|F'(x)| < 1$，但全局不存在统一 $q < 1$。当 $|I_0| < 1$ 时 $I_n \to 0$；当 $I_0 = 1$ 时轨道恒等于 $1$，而 $F'(1) = 2 > 1$，故 $1$ 是排斥不动点。若 $I_0 = -1$，则一步后进入 $1$ 并保持不变；但 $1$ 是排斥不动点，该轨道因精确命中而停留，并非被吸引收敛。若 $I_0 > 1$ 或 $I_0 < -1$，则 $|I_n| = |I_0|^{2^n} \to \infty$。

   因此排斥不动点并不意味着"任何扰动都发散"：例如 $I_0 = 1 - \varepsilon$（$0 < \varepsilon < 1$）的轨道反而趋于 $0$；右侧扰动 $I_0 = 1 + \varepsilon$ 才发散。

2. **临界收敛案例**：$F(x) = \sin x$，$F'(0) = 1$，迭代序列 $x_{n+1} = \sin x_n$ 收敛，但收敛速率为亚线性（$I_n \sim \sqrt{3/n}$，即 $O(n^{-1/2})$）。该次线性速率是离散不动点迭代的经典结果，证明思路与 8.2 节类似，通过 Taylor 展开与 Stolz--Cesàro 定理可得，此处省略详细推导。

---

## 三、方向二：累加求和型（静态积分级数）

### 3.1 定义

单重积分项：

$$
J_k = \int_{L_k}^{U_k} f_k(t) \, dt
$$

有限部分和：

$$
S_n = \sum_{k=1}^n J_k
$$

无穷级数稳态和：

$$
S^* = \sum_{k=1}^\infty J_k
$$

### 3.2 收敛判别规则

静态积分级数本质为数项级数（各项 $J_k$ 为与状态无关的常数），可直接应用经典级数收敛判别法。常用规则如下：

1. **绝对收敛**：级数 $\sum |J_k| < \infty$；
2. **比值判别**：极限 $\lim_{k \to \infty} \big|J_{k+1}/J_k \big| = r < 1$（要求 $J_k \ne 0$）；
3. **莱布尼茨判别**：$J_k$ 正负交替、$|J_k|$ 单调递减、$J_k \to 0$（仅适用于交错级数）。


---

## 四、合并形式（递推+累加混合框架）

### 4.1 定义与收敛定理

迭代公式：

$$
I_{n+1} = \sum_{k=1}^{m_n} \int_{L_{n,k}(I_n)}^{U_{n,k}(I_n)} f_{n,k}(t) \, dt
$$

映射族定义：

$$
F_n(x) = \sum_{k=1}^{m_n} \int_{L_{n,k}(x)}^{U_{n,k}(x)} f_{n,k}(t) \, dt
$$

**退化关系**：所有 $m_n \equiv 1$ 时退化为方向一纯递推模型；若进一步扩展为仿射累加形式 $I_{n+1} = I_n + \sum_{k=1}^{m_n} \int_{L_{n,k}}^{U_{n,k}} f_{n,k}(t) \, dt$，且边界取固定常数时，退化为方向二静态积分级数。正文合并框架为单步多分项的状态更新型递推，是方向一的直接推广。

**定理 4.1（合并形式的收敛性）**：该定理给出非自治状态更新迭代 $I_{n+1} = F_n(I_n)$ 的收敛充分条件。对于定义 1.3 中一般的非自治有限嵌套截断，由于其计算方向及初始数据位置与正向非自治迭代不同，不能直接由本定理推出其收敛性；二者的一致性需要额外的结构条件。自治情形下，在相应初始状态约定下二者可以等价。设 $X = [a, b]$ 为公共前向不变闭集，映射序列 $\{F_n\} \subset C(X)$ 满足：
- **(i) 自映射性**：$\forall n, F_n: X \to X$；
- **(ii) 最终一致 Lipschitz 压缩性**：$\exists N_0, q \in (0, 1), \forall n \ge N_0, \forall x, y \in X, |F_n(x) - F_n(y)| \le q|x - y|$；
- **(iii) 相邻摄动可和性**：$\sum_{n=2}^\infty \sup_{x\in X}|F_n(x) - F_{n-1}(x)| < \infty$。

则存在极限映射 $F: X \to X$，使得 $F_n \rightrightarrows F$（一致收敛），且迭代 $I_{n+1} = F_n(I_n)$ 对任意 $I_0 \in X$ 均收敛到唯一不动点 $I^* = F(I^*)$。该结论是一个充分条件，而非对所有非自治迭代收敛性的充要刻画。

通俗地说，条件 (iii) 要求相邻映射之间差异的总和有限：它排除映射序列的持续大幅周期振荡，但允许随时间的缓慢漂移。

说明：条件 (iii) 保证 $\{F_n\}$ 在 $X$ 上为一致 Cauchy 列，从而存在一致极限 $F$。结合 $F_n(X) \subseteq X$ 与 $X$ 的闭性，进一步有 $F(X) \subseteq X$。统一 Lipschitz 条件可直接传递给极限映射，故 $F$ 本身也是压缩映射。

注：若存在常数 $C>0$ 和 $\lambda \in (0,1)$，使得 $\sup_{x\in X}|F_n(x) - F_{n-1}(x)| \le C\lambda^n$，则条件 (iii) 自动成立。

**维度限定**：本定理条件 (ii) 在一维情形下蕴含于 $|F_n'(x)| \le q$（由拉格朗日中值定理给出）；反之不成立，即 Lipschitz 连续函数未必可导。高维情形需将 $|F_n(x) - F_n(y)| \le q|x - y|$ 替换为算子范数形式（可由雅可比矩阵算子范数有界 $\|J_{F_n}(x)\|_{\mathrm{op}} \le q$ 保证，详见附录 B.2）。映射类由 $C^1(X)$ 放宽为 $C(X)$（仅需连续+一致 Lipschitz），以适配非光滑情形。

#### 主定理证明

**Step 1（序列良定义性与一致极限的存在性）**：由条件 (i) 知 $X$ 为前向不变集，迭代序列 $\{I_n\}$ 始终定义在 $X$ 内，避免定义域逃逸；条件 (ii) 保证 $n \ge N_0$ 后每个 $F_n$ 均为以 $q$ 为系数的压缩映射。

由条件 (iii)，令

$$
\varepsilon_n := \sup_{x\in X}|F_n(x) - F_{n-1}(x)|,
$$

则 $\sum_{n=2}^\infty \varepsilon_n < \infty$。对任意 $m > l \ge 2$，

$$
\sup_{x\in X}|F_m(x) - F_l(x)| \le \sum_{k=l+1}^m \varepsilon_k.
$$

当 $l \to \infty$ 时右边趋于 $0$，故 $\{F_n\}$ 在 $X$ 上为一致 Cauchy 列。由于 $C(X)$ 在一致范数下是 Banach 空间，一致 Cauchy 列收敛，从而存在连续映射 $F: X \to \mathbb{R}$ 使得 $F_n \rightrightarrows F$。再由 $F_n(X) \subseteq X$ 与 $X$ 闭，得 $F(X) \subseteq X$。

**Step 2（迭代序列为 Cauchy 列）**：由于 $I_n \in [a,b]$，对任意 $n$ 都有 $|I_n - I_{n-1}| \le b-a$。记 $C = \max\!\left(b-a,\, |I_{N_0+1} - I_{N_0}|\right)$，以下估计从 $n \ge N_0 + 1$ 开始：

$$
|I_{n+1} - I_n| \le q|I_n - I_{n-1}| + \varepsilon_n.
$$

迭代展开得

$$
|I_{n+1} - I_n| \le q^{n-N_0} C + \sum_{k=N_0+1}^{n} q^{n-k}\varepsilon_k.
$$

第一项对应的几何级数收敛；第二项对 $n$ 求和后可用 Tonelli/非负级数重排得到

$$
\sum_{n=N_0+1}^{\infty} \sum_{k=N_0+1}^{n} q^{n-k}\varepsilon_k \le \frac{1}{1-q} \sum_{k=N_0+1}^{\infty} \varepsilon_k < \infty.
$$

因此 $\sum |I_{n+1} - I_n| < \infty$，从而 $\{I_n\}$ 为 Cauchy 列。又因 $X = [a, b]$ 为 $\mathbb{R}$ 中闭集，完备，故 Cauchy 列 $\{I_n\}$ 收敛于某个 $I^* \in X$。

**Step 3（极限映射的压缩性与连续性）**：由条件 (ii)，对任意 $x, y \in X$ 与所有 $n \ge N_0$，

$$
|F_n(x) - F_n(y)| \le q|x - y|.
$$

令 $n \to \infty$，由 $F_n \rightrightarrows F$ 得

$$
|F(x) - F(y)| \le q|x - y|.
$$

因此极限映射 $F$ 本身是压缩映射（从而自动连续）。唯一性由压缩性保证。

**Step 4（极限交换）**：由 $I_{n+1} = F_n(I_n)$ 且 $I_n \to I^*$，分解

$$
\begin{aligned}
|F_n(I_n) - F(I^*)| &\le |F_n(I_n) - F(I_n)| + |F(I_n) - F(I^*)| \\
&\le \sup_{x\in X}|F_n(x) - F(x)| + |F(I_n) - F(I^*)|.
\end{aligned}
$$

第一项由一致收敛趋于 $0$，第二项由 $F$ 的（Lipschitz）连续性趋于 $0$。因此 $F_n(I_n) \to F(I^*)$。另一方面 $I_{n+1} \to I^*$，故 $I^* = F(I^*)$。

这里没有把"极限与函数交换"作为未经证明的规则，而是由上述估计直接得到。 $\blacksquare$

**与方向一的关系**：方向一为单一固定压缩映射反复迭代，直接适用 Banach 定理；混合框架是一族压缩映射依次迭代，收敛需要额外一致收敛条件。当 $m_n \equiv 1$ 且 $F_n \equiv F$（不随 $n$ 变化）时，合并形式退化为方向一自治递推模型。

**泛函分析背景**：混合求和结构与迭代函数系统（IFS）及非自治离散动力系统存在一定联系；本文的收敛证明主要基于一致 Lipschitz 压缩性、摄动可和性及 Banach 不动点原理。有限 IFS 经典情形参见 Hutchinson (1981) [5]，本文研究的无限映射序列可视为其极限拓展情形。该框架同样可用于非线性微分方程数值迭代求解，提供相应的泛函分析基础。

### 4.2 被积函数依赖状态的导数修正

与主定理的推广关系：本节为 4.1 节主定理在被积函数依赖状态情形下的推广。若 $f_{n,k}$ 依赖当前迭代状态 $I_n$，Leibniz 积分求导法则需补充关于状态变量 $x$ 的偏导数积分项；只要修正后的映射仍满足 4.1 节条件 (ii)（即 $\forall n \ge N_0, \forall x, y, |F_n(x) - F_n(y)| \le q|x - y|$），收敛定理依然成立（条件 (ii) 改用修正后的映射验证）。

若被积函数 $f_{n,k}(t, I_n)$ 依赖当前迭代状态，则 $F_n$ 是状态依赖的非线性积分映射。本节额外假设 $f_{n,k}(t, x)$ 对状态变量 $x$ 连续，且偏导数 $\frac{\partial f_{n,k}}{\partial x}$ 在相关积分域上存在并连续（或满足一致可积条件，例如存在与 $x$ 无关的可积控制函数 $g(t)$ 使得 $\bigl|\frac{\partial f_{n,k}}{\partial x}(t,x)\bigr| \le g(t)$ a.e.），以保证 Leibniz 求导合法；在此基础上，若导数有界则可推出相应的 Lipschitz 条件。这类结构可与 Volterra 型积分方程 [8] 建立联系，但二者并不等同，Leibniz 积分求导法则需增加偏积分项：

$$
\begin{aligned}
F_n'(x) = \sum_{k=1}^{m_n} \Bigg[ &f_{n,k}\bigl(U_{n,k}(x), x\bigr) \cdot U_{n,k}'(x) \\
&- f_{n,k}\bigl(L_{n,k}(x), x\bigr) \cdot L_{n,k}'(x) \\
&+ \int_{L_{n,k}(x)}^{U_{n,k}(x)} \frac{\partial f_{n,k}}{\partial x}(t, x) \, dt \Bigg].
\end{aligned}
$$

### 4.3 非自治收敛判定准则

注：本节给出非自治迭代判据；静态累加求和见 3.2 节。

**准则 4.3.1 全局一致压缩**：$\forall n, x, y$：$|F_n(x) - F_n(y)| \le q|x - y|$（$q < 1$）。证明依据：此为 4.1 节定理条件 (ii) 在 $N_0 = 0$ 时的强版本，但不能单独保证收敛。当 $F_n \equiv F$（自治情形）时退化为 Banach 压缩映射原理；一般非自治情形必须同时满足相邻摄动可和性（条件 iii），方可由定理 4.1 保证收敛到极限映射的不动点。

反例：取 $F_{2n}(x) = 0.5x$，$F_{2n+1}(x) = 0.5x + 0.5$，则每个 $F_n$ 均满足全局压缩（$q = 0.5$），但 $\{F_n\}$ 在 $\mathbb{R}$ 上不收敛到任何映射。对任意初值 $I_0 \in [0, 1]$，序列不收敛于单一状态，而是趋向一个周期为 2 的轨道：偶数下标子列 $I_{2k} \to 2/3$，奇数下标子列 $I_{2k+1} \to 1/3$（由两步复合映射 $x \mapsto 0.25x + 0.5$ 与 $x \mapsto 0.25x + 0.25$ 分别给出，不动点依次为 $2/3$ 与 $1/3$）。这表明单独的全局一致压缩不能保证收敛到单一极限。

更一般地，对任意 $0 < q < 1$ 及 $a \neq 0$，设 $F_n(x) = qx + (-1)^n a$，则每个 $F_n$ 都是同一压缩常数 $q$ 的压缩映射，但迭代 $I_{n+1} = qI_n + (-1)^n a$ 的偶、奇子列分别趋于 $\mp a/(q+1)$，形成振幅不消的振荡。此例表明：统一压缩只控制"每一步的收缩强度"，但无法抑制映射本身持续的系统性摆动；要排除这种振荡，必须对相邻映射的差异加以控制，这正是定理 4.1 中相邻摄动可和条件 (iii) 的作用。上述例子在 $\mathbb{R}$ 上构造；在有界闭区间上也可通过截断或适当限制得到类似的有界振荡例子，结论同样成立。

**准则 4.3.2 渐近压缩**：若 $F_n \in C^1(X)$ 且

$$
\limsup_{n\to\infty} \sup_{x\in X}|F_n'(x)| = q_\infty < 1,
$$

则存在 $N_0$ 和 $q \in (q_\infty, 1)$，使得对所有 $n \ge N_0$ 有 $|F_n'(x)| \le q$，从而由中值定理得到最终一致 Lipschitz 压缩。但该条件本身不能保证迭代收敛；若要调用定理 4.1，还必须同时满足其余条件，特别是相邻摄动可和性以及公共自映射性。满足这些条件后，定理 4.1 给出收敛到极限映射 $F$ 的唯一不动点。

**准则 4.3.3 极限映射收敛**：设存在公共前向不变闭集 $X$，并且 $F_n: X \to X$；若 $F_n \to F$ 在 $X$ 上一致收敛，且存在统一常数 $q < 1$ 使

$$
|F_n(x) - F_n(y)| \le q|x - y|, \qquad x, y \in X,
$$

则由 $F_n \to F$ 逐点收敛与上述一致 Lipschitz 界，取极限即得 $F$ 也是 $q$-Lipschitz 压缩映射；结合 $F_n(X) \subseteq X$ 与 $X$ 闭性得 $F(X) \subseteq X$，由 Banach 原理 $F$ 存在唯一不动点 $I^*$。

此处收敛性需单独证明，不能直接套用定理 4.1 的证明（定理 4.1 依赖相邻摄动可和性以推出一致极限的存在，本准则直接假设一致收敛，是不同的充分条件）。

在上述条件下，由 $F_n(X) \subseteq X$ 及 $I_0 \in X$ 归纳可知 $I_n \in X$（$\forall n \ge 0$），故以下估计均在 $X$ 内进行。记 $a_n := \sup_{x\in X}|F_n(x) - F(x)| \to 0$，$e_n := |I_n - I^*|$，则

$$
e_{n+1} = |F_n(I_n) - F(I^*)| \le |F_n(I_n) - F(I_n)| + |F(I_n) - F(I^*)| \le a_n + qe_n
$$

由 $0 < q < 1$ 且 $a_n \to 0$：任给 $\epsilon > 0$，取 $N$ 使 $n \ge N$ 时 $a_n < \epsilon(1-q)/2$，展开递推式得 $\limsup_n e_n \le \epsilon/2$，故 $e_n \to 0$，即非自治迭代收敛到 $F$ 的唯一不动点。若仅知道一致收敛而没有统一压缩常数，不能据此推出本文的收敛结论。

注：本准则仅给出非自治迭代收敛的充分条件，并非必要条件。

**注 4.3.4（单调性与压缩性）**：单调性本身不构成收敛的充分条件。例如 $F(x) = -x$ 于 $[-1, 1]$ 单调递减且自映射，但迭代 $I_{n+1} = -I_n$ 对 $I_0 \neq 0$ 永久振荡不收敛。本框架下的收敛性由一致压缩性保证；单调性仅作辅助分析，不可作为独立判据。

### 4.4 迭代失效边界与判据适用范围

本节所称"失效"首先指本文的压缩型充分条件无法直接应用，并不意味着对应迭代必然发散。具体包括：

1. 尾部无法建立统一的压缩常数，即不存在 $q < 1$ 使 $|F_n(x) - F_n(y)| \le q|x - y|$ 对所有充分大的 $n$ 成立；
2. 不存在公共前向不变区间，或在所采用的模型语义中要求 $L_n(x) \le U_n(x)$ 而该约束失效。若采用有向积分，$L_n(x) > U_n(x)$ 本身并非积分定义错误，而表示积分方向反转；
3. 若极限映射 $F$ 在不动点 $I^*$ 处可导，且满足 $|F'(I^*)| > 1$，则该不动点为局部排斥点；若 $F$ 仅 Lipschitz 连续而不可微，则局部排斥性需改用相应邻域上的最小可用 Lipschitz 常数大于 $1$ 来刻画。这只排除了从该点附近稳定收敛到该不动点的可能性，并不排除轨道收敛到其他吸引不动点、周期轨道，或其他由具体动力系统决定的极限行为；
4. $F_n$ 不满足定理 4.1 所需的相邻摄动可和性条件时，本文定理不能用于推出非自治迭代的收敛性；此时需要其他动力系统工具或更强的具体结构分析（具体反例参见 4.3.1 节周期振荡映射）。


---

## 五、数值实现方案

### 5.1 迭代停止判定标准

1. **绝对残差**：$|I_{n+1} - I_n| < \varepsilon_1$ —— 适用于迭代值量级较小的场景；
2. **相对残差**：$|I_{n+1} - I_n|/|I_n| < \varepsilon_2$（仅在 $I_n \ne 0$ 时使用；否则退化为绝对残差）—— 适用于迭代值量级较大的场景，避免精度随幅值漂移；
3. **不动点残差**：$|I_{n+1} - F(I_{n+1})| < \varepsilon_3$ —— 直接衡量当前值与稳态的偏差，精度要求高时优先使用（主要适用于自治情形，或非自治迭代已接近极限映射的后期阶段；一般非自治情形更推荐使用相邻残差 $|I_{n+1} - I_n|$）。

### 5.2 初始值选取策略

1. $F$ 单调递增：取 $I_0 = 0$ 或区间边界；
2. $F$ 单调递减：多组初值验证交替振荡收敛；
3. 二分法定位安全可行区间，取区间中点作为初始迭代值。

### 5.3 迭代伪代码

```
输入: I0, ε, N_max, {F_n}  // 自治情形取 F_n ≡ F
输出: I*
1. I_prev = I0
2. for n = 1 to N_max:
3.     I_curr = F_n(I_prev)  // 按定义计算对应的变限积分
4.     if |I_curr - I_prev| < ε: break  // 可根据第 5.1 节选择绝对残差、相对残差或不动点残差
5.     I_prev = I_curr
6. return I_curr
```

### 5.4 数值误差耦合说明

若每一步实际计算的映射 $\widetilde{F}_n$ 满足统一误差界 $\sup_{x\in X}|\widetilde{F}_n(x) - F_n(x)| \le \delta$ 且相应迭代具有统一压缩常数 $q < 1$，则在标准扰动分析下，稳态误差量级可由几何级数控制，典型上界为

$$
\text{Total Error} \le \frac{\delta}{1-q}.
$$

该估计依赖于统一的单步误差界与压缩性，不能脱离这些条件单独使用。特别地，当 $q \to 1^{-}$ 时，扰动放大因子 $(1-q)^{-1}$ 增大，系统对模型误差、截断误差及数值扰动更加敏感。

注：该误差界主要针对自治迭代；非自治情形需所有 $F_n$ 具有统一压缩常数 $q$ 与统一单步误差界 $\delta$，结论方可沿用。

**有限 $n$ 版本**：在实际数值迭代 $\widetilde{I}_{n+1} = \widetilde{F}_n(\widetilde{I}_n)$ 中，若 $\sup_{x\in X}|\widetilde{F}_n(x) - F_n(x)| \le \delta_n$，记

$$
\widetilde{e}_n := |\widetilde{I}_n - I^*|, \qquad a_n := \sup_{x\in X}|F_n(x) - F(x)|,
$$

则在定理 4.1 的最终一致压缩条件下（即 $|F_n(x) - F_n(y)| \le q|x-y|$ 对 $n \ge N_0$ 成立），对 $n \ge N_0$ 有

$$
\widetilde{e}_n \le q^{n-N_0} \widetilde{e}_{N_0} + \sum_{k=N_0}^{n-1} q^{n-1-k}(\delta_k + a_k),
$$

其中 $q^{n-1-k}$ 为误差传播的几何衰减权重（$k$ 越靠近 $n-1$，权重越接近 $1$；早期误差经多步压缩后权重衰减为 $q^{n-1-k}$）。当 $n \to \infty$ 时，若 $\delta_k \le \delta$ 一致有界且 $a_k \to 0$，上式退化为稳态界 $\widetilde{e}_n \le \delta/(1-q) + o(1)$。

**工程实用建议**：优先选用辛普森积分、高斯-勒让德求积；单步积分误差控制在 $10^{-6}$ 量级；若压缩常数 $q$ 接近 1（如 $q = 0.99$），需将单步误差压缩至 $10^{-8}$，才能保证最终结果精度 $10^{-6}$。

---

## 六、模型的可配置性与领域迁移

说明：本章中的应用场景均用于展示嵌套积分结构在不同领域中的潜在表达形式，仅属于结构性类比。本文不针对具体领域建立完整数学模型，不涉及参数辨识、实验验证或实际系统预测；相关应用的严格建模需结合具体领域背景进一步研究。

本嵌套积分结构核心递推公式固定不变：

$$
I_{n+1} = \int_{L(I_n)}^{U(I_n)} f(t) \, dt
$$

仅通过替换四大核心组件，即可适配完全不同行业、不同类型动力学问题：

1. **被积函数 $f(t)$**：系统响应函数、概率分布密度、动力学方程；
2. **边界函数 $L(x), U(x)$**：当前系统状态对应的取值范围、可行区间；
3. **递推层数 $N$**：迭代计算深度、系统演化周期；
4. **初始值 $I_0$**：系统初始状态、计算起始条件。

替换组件后，同一数学框架从结构形式上可用于构造库存管理、信号滤波、化学振荡反应、生态种群演化、自适应自动控制等动态系统的数学模型。结构本身不绑定任意特定行业，仅提供"状态 $\to$ 积分边界 $\to$ 积分累积更新状态"通用计算范式。具体适用性需结合领域模型进一步验证。

以下列举四类按本结构核心规则自然涌现的典型应用场景，各场景仅需替换被积函数 $f$、边界映射 $L, U$ 与状态语义 $I_n$：

1. **动态库存控制（概念映射）**：$I_n$ 为第 $n$ 周期期末库存，$f(D)$ 为需求概率密度，$L(I_n), U(I_n)$ 为当前库存水平触发的补货上下界；具体成本优化与补货量计算需结合业务模型进一步展开；
2. **自适应实验设计（概念映射）**：$I_n$ 为累积样本量或信息熵，$f(t)$ 为信息增益密度，边界由当前认知状态动态决定；具体信息增益函数需结合实验设计理论另行构造；
3. **多阶段信号处理（概念映射）**：$I_n$ 为滤波器状态或信噪比估计，$f(t)$ 为噪声功率谱密度，积分区间对应自适应门限；具体滤波器设计需结合信号处理理论另行验证；
4. **多因子定价模型（概念映射）**：$I_n$ 为风险因子累积暴露，$f(t)$ 为因子收益分布，边界由当前组合风险预算动态生成；具体风险预算分配需结合金融模型另行推导。

---

## 七、与经典数学概念差异化对比

| 对比对象 | 核心区分点 |
|:---|:---|
| 变上限积分 | 积分上限为自变量 $x$，输出为函数；本结构边界为前一步数值，输出一维数值序列 |
| 多重累次积分 | 内层积分结果作为外层被积函数；本结构内层结果作为外层积分上下限 |
| 积分方程 | 求解未知函数 $y(x)$；本结构求解一维实数迭代序列 |
| Picard 迭代 | 迭代变量为函数空间元素；本结构迭代变量为实数 |
| Newton-Raphson | 二阶收敛特殊不动点迭代，用于方程求根；本结构通用压缩映射迭代 |
| 一维离散函数迭代 | $x_{n+1}=F(x_n)$（经典情形下 $F$ 固定不变）；本结构 $F$ 内部包含积分运算，积分边界由状态动态构成 |

---

## 八、基准算例

本节通过两类典型算例展示动态边界积分迭代在不同几何配置下的行为差异。

### 8.1 算例 A：标准压缩收敛（Omega 常数）

设置被积函数 $f(t) = e^{-t}$，积分下限 $L = I_n$，积分上限为正无穷，迭代公式：

**说明（Remark）**：本算例积分上限为 $+\infty$，边界函数 $U_n$ 不满足 H2 的 $C^1$ 假设；但本算例通过直接显式计算 $F(x) = e^{-x}$ 绕开了 Leibniz 求导路径，故 H2 在此算例中无需成立。对于一般情形，H2 仍作为默认假设。

$$
I_{n+1} = \int_{I_n}^{+\infty} e^{-t} \, dt = e^{-I_n}
$$

求导 $F'(x) = -e^{-x}$。在整个 $x > 0$ 上虽然有 $|F'(x)| < 1$，但这并不足以直接给出一个统一的压缩常数。为应用 Banach 压缩映射原理，可取前向不变闭区间

$$
X = [e^{-1}, 1].
$$

此时

$$
F(X) = [e^{-1}, e^{-e^{-1}}] \subseteq X,
$$

且

$$
\sup_{x\in X}|F'(x)| = e^{-e^{-1}} \approx 0.6922 < 1.
$$

因此 $F$ 在 $X$ 上是严格压缩映射。不动点方程 $I^* = e^{-I^*}$，变形 $I^* e^{I^*} = 1$，解析解为 Omega 常数 $\Omega = W(1) \approx 0.567143$ [4]。不动点处 $|F'(I^*)| = \Omega \approx 0.567 < 1$，迭代在该固定点附近交替振荡并线性收敛。

### 8.2 算例 B：临界次线性收敛（积分结构原型）

设置同一被积函数 $f(t) = e^{-t}$，但积分区间改为有限区间 $[0, I_n]$，迭代公式：

$$
I_{n+1} = \int_{0}^{I_n} e^{-t} \, dt = 1 - e^{-I_n}
$$

不动点方程 $I^* = 1 - e^{-I^*}$ 在 $\mathbb{R}$ 上**仅有唯一解 $I^* = 0$**。此处需作零点分析而非仅依赖凹性：$g(x) = 1 - e^{-x} - x$ 满足 $g'(x) = e^{-x} - 1$，故 $g$ 在 $x > 0$ 上严格递减、在 $x < 0$ 上严格递增，在 $x = 0$ 处取得全局最大值 $g(0) = 0$，从而其余各处 $g(x) < 0$（参见附录 C.3 的完整论证）。此处 $F'(0) = 1$，不满足严格压缩条件 $|F'| < 1$，故 Banach 定理不直接适用。

**收敛行为**：若 $I_0 = 0$，则 $I_1 = 0$，轨道恒为 $0$（平凡收敛）。对任意 $I_0 > 0$，有 $0 < I_{n+1} < I_n$，且 $I_n \downarrow 0$。其收敛速度为次线性 $I_n \sim 2/n$。若 $I_0 < 0$，由 $g(x)=1-e^{-x}-x<0$ 对所有 $x<0$ 成立，可知序列严格递减；若无下界则发散至 $-\infty$，若有下界则收敛到某个 $L<0$，但不动点方程 $L=1-e^{-L}$ 在 $L<0$ 时无解，矛盾。因此 $I_n \downarrow -\infty$，序列发散。故收敛域为 $[0, +\infty)$。

**渐近证明**：由 Taylor 展开，在 $I_n \to 0$ 时

$$
I_{n+1} = 1 - e^{-I_n} = I_n - \frac{1}{2}I_n^2 + O(I_n^3).
$$

因此

$$
\frac{1}{I_{n+1}} - \frac{1}{I_n} = \frac{I_n - I_{n+1}}{I_n I_{n+1}} = \frac{\frac{1}{2}I_n^2 + O(I_n^3)}{I_n\left(I_n - \frac{1}{2}I_n^2 + O(I_n^3)\right)} \longrightarrow \frac{1}{2}.
$$

由 Stolz--Cesàro 定理，

$$
\lim_{n\to\infty} \frac{1/I_n}{n} = \frac{1}{2},
$$

从而

$$
\boxed{I_n \sim \frac{2}{n}}.
$$

这给出了临界情形下的严格次线性渐近率；该结论不由 Banach 压缩定理给出。

### 8.3 对比意义

两算例采用同一被积函数 $f(t) = e^{-t}$，但积分边界几何截然不同：
- **算例 A**（半无限区间 $[I_n, +\infty)$）：产生严格压缩映射，Banach 定理直接保证线性收敛
- **算例 B**（有限区间 $[0, I_n]$）：产生临界映射 $F'(0) = 1$，收敛存在但速率为次线性

这说明积分边界的几何结构能够显著改变诱导动力系统的收敛性质，也展示了本文框架对"标准收敛"与"临界行为"的统一描述能力。


---

## 附录

### A. 库存控制模型示意

考虑动态库存控制场景：设 $I_n$ 为第 $n$ 周期期末库存，需求随机变量 $D$ 服从概率密度 $f(D)$。补货策略的上下界由当前库存水平动态决定，记为 $L(I_n)$ 与 $U(I_n)$。在此框架下，下期库存 $I_{n+1}$ 由该周期内的期望需求与具体的补货执行规则共同决定。本文聚焦于该迭代映射的收敛性理论，具体的库存成本优化与补货量计算依赖于所选的业务模型，此处不再展开具体积分形式。

### B. 高阶拓展研究方向

**B.1 随机扰动拓展**：$I_{n+1} = F(I_n) + \xi_{n+1}$，研究有界噪声下系统稳定性与稳态渐近分布。

**B.2 高维向量拓展**：迭代变量 $I_n \in \mathbb{R}^d$，收敛条件替换为雅可比矩阵 $J_{F_n}(x)$ 的算子范数 $\|J_{F_n}(x)\|_{\mathrm{op}} \le q < 1$；在 $X$ 为凸集且 $F$ 在 $X$ 上为 $C^1$ 时，可沿连接 $x, y$ 的线段积分得到均值不等式 $\|F(x) - F(y)\| \le \sup_{z\in X}\|J_F(z)\|_{\mathrm{op}} \cdot \|x - y\|$。更一般地，可用适当的路径连通条件以及相应的路径长度界替代凸性；本文仅采用凸集这一标准充分条件。

注：谱半径 $\rho(J) = \lim_{n\to\infty}\|J^n\|^{1/n}$ 仅刻画长迭代的渐近行为，不能直接给出 Lipschitz 常数；高维压缩映射的判定需用算子范数。

**B.3 Volterra 积分关联**：某些状态依赖的积分映射与 Volterra 型积分方程存在形式上的联系，但二者并不等同；连续时间模型还需额外引入时间变量、核函数及相应的函数空间。

**B.4 自回归模型类比 [7]**：AR($p$) 模型与本结构都可表示为离散状态更新，但 AR($p$) 的线性组合/随机扰动机制与本结构的变限积分机制不同，此处仅作形式类比。

**B.5 分形迭代系统 [9]**：自相似映射序列结合 IFS 研究分形不动点。

**B.6 算子方程推广**：映射 $F: \mathcal{H} \to \mathcal{H}$，用于偏微分方程迭代求解。

**B.7 教学习题设计**：可设计概念验证、数值计算、理论证明、工程建模四类习题。

**B.8 离散-连续退化桥梁（ODE 退化推导）**：离散迭代退化为连续动力系统需满足一阶相容性条件：

$$
F_{\Delta t}(I) = I + \Delta t \cdot G(I) + o(\Delta t) \quad (\Delta t \to 0)
$$

这里 $F_{\Delta t}$ 是离散演化映射，与正文的积分算子 $F$ 不同。该条件是导出形式上连续时间极限 $\dot{I} = G(I)$ 所需的一阶一致性要求（first-order consistency condition）。但仅凭此条件，本身不能保证离散轨迹收敛到对应的连续时间解；还需要稳定性及额外的正则性假设。

**B.9 累乘与广义累积拓展（核心前置约束）**

本节中 $G(x)$ 表示形如 $\displaystyle\int_{L(x)}^{U(x)} f(t)\,dt$ 的变限积分映射，其形式与正文中的自治映射 $F$（或非自治映射 $F_n$）一致，此处为区分复合结构单独记作 $G$。

**前置约束与收敛范畴**：本节所有推广形式均建立在第 2.1 节定义的前向不变闭集之上。

- **全局收敛**：需存在闭区间 $X = [a, b]$，使得映射 $F: X \to X$ 且 $\forall x \in X$ 上的积分均按本文采用的积分约定良定义；若采用无定向积分语义，则另需 $L(x) \le U(x)$。
- **局部收敛**：若仅能在不动点 $I^*$ 的某邻域 $U \subset X$ 内满足上述条件，则仅保证局部收敛性。

特别地，对于累乘迭代 $F(x) = xG(x)$，若 $I^* = 0$ 为不动点，严禁仅校验单点合法性。必须验证在 $x = 0$ 的邻域内 $F(x)$ 的有界性：不仅要保证 $\lim_{x \to 0} x \cdot G(x) \in X$（必要性），还需核验该邻域内全局值域约束（充分性），以规避 $G(x)$ 无界导致的区间溢出或数值震荡风险。

本结构默认采用加法累积形式 $I_{n+1} = G(I_n)$。以下给出乘法与仿射形式的推广。

#### B.9.1 累乘特例

定义 $F(x) = x \cdot G(x)$，迭代式为 $I_{n+1} = F(I_n)$。

**压缩条件**：$|F'(x)| = |G(x) + xG'(x)| \le q < 1$。

**不动点分析**：方程 $I^* = I^* \cdot G(I^*)$ 的解为 $I^* = 0$ 或 $G(I^*) = 1$。

对于 $I^* = 0$ 的情形，除校验 $L(0) < U(0)$ 外，必须依据前述前置约束验证邻域不变性。需注意，$\lim_{x \to 0} xG(x) \in X$ 仅是必要性层面的启发式检查，并不能替代定义域与自映射性的验证。例如 $G(x) = 1/\sqrt{x}$ 仅在 $x > 0$ 上定义，因此不能在不另行定义延拓的情况下把 $x = 0$ 当作该 $G$ 的合法输入。若在 $x > 0$ 上考虑 $F(x) = xG(x) = \sqrt{x}$，则在 $x = 0.04$ 处 $F(x) = 0.2$；若 $X = [0, 0.1]$，则 $F(X) \not\subseteq X$。这个例子说明必须先明确映射的定义域，再验证前向不变性；若希望把 $0$ 纳入状态空间，则应明确把 $F$ 连续延拓为 $F(0) = 0$，此时研究对象已是延拓后的 $F$，而不是原始的 $xG(x)$ 在 $x = 0$ 的值。延拓后必须重新验证该映射在包含 $0$ 的闭集上的 Lipschitz/压缩常数以及值域不变性，不得直接把原开集上的性质自动继承给延拓后的映射。

**稳定性**：由 $|F'(I^*)|$ 决定局部吸引性或排斥性（参见 2.3 节）。

**算例**：$G(x) = e^{-x}$，迭代 $I_{n+1} = I_n e^{-I_n}$，对应 $F(x) = x e^{-x}$，导数为 $F'(x) = (1-x)e^{-x}$。在 $x = 0$ 处 $|F'(0)| = 1$，不满足严格压缩条件（临界情形）。方程 $G(I^*) = 1$ 仅有解 $I^* = 0$，故不存在非零不动点。事实上，对 $I_0 > 0$，序列单调递减趋于 $0$，收敛速度为 $O(1/n)$（由差分渐近 $I_{n+1}^{-1} - I_n^{-1} \to 1$ 可得），属于次线性临界收敛，无法由 Banach 定理保证速率。本算例说明乘性结构不能直接套用加法情形的压缩条件，需对复合映射 $F$ 单独验证。

**数值注记**：乘性迭代的误差传播呈乘性放大特征，其总误差上界形式可能异于加法迭代的 $\delta/(1-q)$，需视 $G(x)$ 特性另行推导，但第五章的误差控制思想依然适用。

#### B.9.2 仿射结构

定义 $F(x) = \alpha x + \beta G(x)$，迭代式为 $I_{n+1} = F(I_n)$。

**压缩条件**：$|F'(x)| = |\alpha + \beta G'(x)| \le q < 1$。

注：$\alpha + \beta = 1$ 为工程稳态常用约束，非收敛必要条件。

**算例**：取 $\alpha = 0.6$，$\beta = 0.4$，$G(x) = \int_0^x e^{-t} \, dt$。对 $x > 0$ 有 $F'(x) = 0.6 + 0.4e^{-x} < 1$；在 $x = 0$ 处 $F'(0) = 1$，为临界情形，需单独验证邻域不变性（如直接计算 $F(x) - x = 0.6x + 0.4(1 - e^{-x}) - x = 0.4(1 - e^{-x} - x)$，由附录 C.3 已知 $g(x) = 1 - e^{-x} - x < 0$ 对 $x > 0$ 成立，故在不动点右侧邻域内 $F(x) < x$，迭代单调下降）。不动点方程 $0.6I^* + 0.4(1 - e^{-I^*}) = I^*$ 等价于 $I^* = 1 - e^{-I^*}$，因此在 $\mathbb{R}$ 上唯一不动点仍为 $I^* = 0$；这里无需另作数值求解。

**数值注记**：乘性迭代建议采用相对残差准则（见 5.1 节）。

#### B.9.3 广义运算收敛原则

迭代式 $I_{n+1} = I_n \oplus G(I_n)$，构造 $F(x) = x \oplus G(x)$，收敛核心为 $\sup |F'(x)| < 1$。

注：$\max / \min$ 非光滑运算不在本微分框架内。

**非自治拓展**：若系数或核函数时变（如 $G_n(x)$），收敛判定需参照第 4.3 节非自治准则。

**高维拓展**：向量情形需将 $|F'(x)|$ 替换为 Jacobian 算子范数 $\|J_F(x)\|_{\mathrm{op}}$，详见附录。

**求导范式**：对于具体运算 $\oplus$，复合映射 $F(x) = x \oplus G(x)$ 的导数需依据 Leibniz 积分法则及相应的二元运算求导法则具体计算。

### C. 常见计算与认知误区（实操导向）

C.1 **积分方向与模型语义混淆**：$L(x) > U(x)$ 在有向积分约定下并非数学错误，而会使积分符号反转；但在库存、可行域等具有"下界/上界"语义的应用中，可能失去模型意义。预防：先明确采用有向积分还是无定向区间约束，再相应验证 $L(x) \le U(x)$。

C.2 **层级编号颠倒错误**：1.2 节嵌套下标 $k$ 越大越靠内，与递推下标 $I_0 \to I_1 \to \cdots$ 方向相反，易在移植代码时混淆。预防：严格使用本文档 1.2 节的下标约定。

C.3 **算例数值陷阱**：形如 $F(x) = 1 - e^{-x}$（$L = 0, U = x$）的唯一实不动点为 $I^* = 0$，读者需注意避免将 Omega 常数的数值错误套用到本算例。验证：$g(x) = 1 - e^{-x} - x$ 满足 $g(0) = 0$ 且 $g'(x) = e^{-x} - 1$，故 $g(x)$ 在 $x > 0$ 上严格递减；结合 $x < 0$ 时 $1 - e^{-x} - x < 0$，可知唯一实根为 $0$。此外 $F'(0) = 1$，属于临界情形。预防：代入具体 $F$ 重新求解不动点方程 $I^* = F(I^*)$，不要套用其他算例的数值。

C.4 **混淆"无限嵌套"与"一般无限重积分"**：本文定义的无限嵌套极限是截断值序列 $I^{(N)}$ 的极限，其收敛可由第 4.1 节的压缩型充分条件保证。一般的无限级数、无限层迭代或其他无限重积分问题各自需要相应的收敛条件，不能由本文定理自动推出。预防：在 1.3 节框架下使用前先明确截断序列及其收敛条件。

C.5 **维度误用**：将一维压缩条件 $|F'(x)| \le q$ 直接套用于多维向量场，遗漏 Jacobian 算子范数条件。预防：高维情形详见附录 B.2。

C.6 **数值停止准则误选**：$|I_n|$ 接近 $0$ 时相对残差 $\frac{|I_{n+1} - I_n|}{|I_n|}$ 会失真。预防：根据 5.1 节场景说明选用绝对/相对/不动点残差。

---

## 开放问题与未来拓展

以下方向留待后续研究：

1. 状态依赖被积函数的非连续情形正则性理论
2. 非自治无限嵌套截断与递推序列对应关系的充要条件
3. 应用场景的显式数值验证
4. 高维与复分析拓展
5. 非压缩型（临界）收敛判据
6. 迭代序列收敛的充要条件的完整刻画（呼应定义 1.3 注，目前仅给出充分条件）

---

## 参考文献

[1] Banach, S. (1922). Sur les opérations dans les ensembles abstraits et leur application aux équations intégrales. *Fundamenta Mathematicae*, 3, 133--181.

[2] Ostrowski, A. M. (1973). *Solution of Equations in Euclidean and Banach Spaces* (3rd ed.). New York: Academic Press.

[3] Kantorovich, L. V., & Akilov, G. P. (1982). *Functional Analysis* (2nd ed.). Oxford: Pergamon Press.

[4] Corless, R. M., Gonnet, G. H., Hare, D. E. G., Jeffrey, D. J., & Knuth, D. E. (1996). On the Lambert W function. *Advances in Computational Mathematics*, 5(1), 329--359.

[5] Hutchinson, J. E. (1981). Fractals and self-similarity. *Indiana University Mathematics Journal*, 30(5), 713--747.

[6] Berinde, V. (2007). *Iterative Approximation of Fixed Points* (2nd ed.). Lecture Notes in Mathematics, Vol. 1912. Berlin: Springer.

[7] Elaydi, S. (2005). *An Introduction to Difference Equations* (3rd ed.). New York: Springer.

[8] Burton, T. A. (2005). *Volterra Integral and Differential Equations* (2nd ed.). Amsterdam: Elsevier.

[9] Barnsley, M. F. (1988). *Fractals Everywhere*. Boston: Academic Press.
